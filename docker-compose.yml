version: "3.9"
services:
  redis:
    image: redis
    ports:
      - "6379:6379" # remove me in prod
  gameserver:
    build: gameserver
    ports:
      - "5000:5000" # remove me in prod
    depends_on:
      - redis
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    networks:
      - default
      - gamenet
  webserver:
    build: webserver
    ports:
      - "80:80"
    depends_on:
      - redis
    environment:
      - RC3_CLIENT_ID=EQilXFujAiRa3yj0Sli3OanTZg5apPO0ccpYi4Sp
      - RC3_REDIRECT_URI=https://waboorrt.flipdot.org/login
      - RC3_TOKEN_URI=https://waboorrt.flipdot.org/token
  gitserver:
    build: gitserver
    ports:
      - "2222:22"
    volumes:
      - botvolume:/git
      - githome:/home/
      - gitopt:/opt
      - botbuilder-notify:/botbuilder
  botbuilder:
    build: botbuilder 
    volumes:
      - botbuilder-notify:/botbuilder
      - botvolume:/git
      - /var/run/docker.sock:/var/run/docker.sock
  scoreserver:
    build: scoreserver
    depends_on:
      - gameserver
      - redis

networks:
  gamenet:
    external: true
volumes:
  botvolume:
  githome:
  gitopt:
  botbuilder-notify:
